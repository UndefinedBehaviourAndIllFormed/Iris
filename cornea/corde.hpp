#pragma once
#include <common/types.hpp>

namespace iris::cornea
{
    namespace _0_0_1
    {
        enum class corde : u8
        {
            general,
            compilation_unit,
            global_name,
            global_version,
            local_name,
            local_version
        };
    }
}